#include "parse.hpp"

namespace testCompiler {
    namespace parser {
        int labelp = -1;
        bool isOK = true;
        
        void program(std::vector<lexer::Word>& wordList) {
            
        }
    }
}